.scroll-down-btn {
	background-color: transparent;
	display: flex;
	align-items: center;
	color: $text-color;
	cursor: pointer;
	&__icon {
		width: 14px;
		height: 14px;
		fill: $text-color;
		margin-left: 15px;
	}
}
