.skill{
	&-component {
		color: #373E42; 
		display: flex;
  		align-items: center;
	}

}
.title {
  font-size: 16px;
  flex: 1;
}

.buttons {
  display: flex;
  align-items: center;
}

.percent {
  margin: 0 30px;
}

.btn {
  margin-right: 22px;
  background-color: transparent;

  &:last-child {
    margin-right: 0;
  }
}